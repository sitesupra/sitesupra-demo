configuration:
    prefix:                 /
    defaults:
        foo: bar
        buz: 123
routes:
    cms_root:
        pattern: /cms
        controller: cmsController
        filters:
            - cmsauth
        defaults:
            foo: bar_override
            