if(typeof console != 'undefined' ) {

	{% for blockStats in debugData %}				
		console.log({{ blockStats.totals | json_encode | raw}});
		
		{% for actionValue in blockStats.actions %}
			console.log({{ actionValue | json_encode | raw}});
		{% endfor %}
		
		console.log(' ');
		
	{% endfor %} 

}

