configuration:
    prefix:                 %cms.prefix%
    defaults:
        foo: bar
        buz: 123
routes:
    cms_root:
        pattern:            /
        controller:         CmsController
        action:             index
        filters:
            - cmsauth
        defaults:
            foo: bar_override
            