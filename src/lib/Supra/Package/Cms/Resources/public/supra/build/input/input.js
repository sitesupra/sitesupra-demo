YUI.add("supra.input", function (Y) {
	//Invoke strict mode
	"use strict";
	
	//supra.input is just a wrapper for everything else
	//should this be changed to alias instead?
	
}, YUI.version, {requires:["supra.form"]});